PORT=8080
POSTGRES_PORT=5433
DATABASE_URL=postgres://ingest:ingest@localhost:5433/ingest?sslmode=disable
INGEST_PUBLIC_KEY_FILE=keys/public.pem

# Optional: for `make bootstrap` / `make pull` on private repos (do not commit real tokens)
# GITHUB_TOKEN=ghp_...
